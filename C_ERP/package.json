{
  "name": "nexaerp",
  "version": "1.0.0",
  "private": true,
  "description": "NexaERP multi-tenant SaaS ERP monorepo.",
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "corepack npm run build --workspace @nexaerp/shared-types && corepack npm run build --workspace @nexaerp/api && corepack npm run build --workspace @nexaerp/web && corepack npm run build --workspace @nexaerp/desktop",
    "lint": "corepack npm run lint --workspace @nexaerp/shared-types && corepack npm run lint --workspace @nexaerp/api && corepack npm run lint --workspace @nexaerp/web && corepack npm run lint --workspace @nexaerp/desktop",
    "test": "corepack npm run test --workspace @nexaerp/shared-types && corepack npm run test --workspace @nexaerp/api && corepack npm run test --workspace @nexaerp/web && corepack npm run test --workspace @nexaerp/desktop",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,css,yml,yaml}\"",
    "typecheck": "corepack npm run build --workspace @nexaerp/shared-types && corepack npm run typecheck --workspace @nexaerp/api && corepack npm run typecheck --workspace @nexaerp/web && corepack npm run typecheck --workspace @nexaerp/desktop"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.21",
    "@nestjs/schematics": "^11.1.0",
    "@types/node": "^25.9.1",
    "prettier": "^3.3.3",
    "ts-node": "^10.9.2",
    "turbo": "^2.0.11",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "inquirer": "^13.4.3"
  }
}
