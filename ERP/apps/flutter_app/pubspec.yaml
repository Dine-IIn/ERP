name: enterprise_erp_app
description: Enterprise ERP client for Android, iOS, Windows, and macOS.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  drift: ^2.22.1
  drift_flutter: ^0.2.2
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2
  sqlite3_flutter_libs: ^0.5.26
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.13
  drift_dev: ^2.22.1
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
